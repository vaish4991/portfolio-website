#include <stdio.h>
int main() {
    int num;
    printf("enter the number less than 10:");
    scanf("%d" ,&num);
    if(num <= 10)
    printf("what obedient servant you are!");
    else
    printf("Digit you emtered is greater than 10");

}
