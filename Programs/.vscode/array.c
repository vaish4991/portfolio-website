#include <stdio.h>
int main() {
    int numbers[5] = {10, 20, 30, 40,50}; //intialise the array
// access and print array elements
for (int i = 0; i<5; i++) {
    printf("Element %d\n",numbers[i]);
}
return 0;

}
