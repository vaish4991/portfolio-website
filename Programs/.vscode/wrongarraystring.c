#include <stdio.h>
int main() {
    ames[5] = {nayan, maya, diya, neha, john}; //intialise the array
// access and print array elements
for (int i = 0; i < 5; i++) {
    printf("Element %d: %d\n", i, ames[i]);
}
return 0;

}
