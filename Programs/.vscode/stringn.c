#include <stdio.h>
int main() {
    int names[5] = {nayan, maya, diya, neha, john}; //intialise the array
// access and print array elements
for (int i = 0; i < 5; i++) {
    printf("Element %s: %s\n", i, names[i]);
}
return 0;

}
